# Bootstrap download verification — pinned SHA-256 digests.
#
# pixel-bootstrap.sh verifies network-fetched setup scripts against these
# digests before installing them. This file is the source of truth for the
# digests embedded in pixel-bootstrap.sh; the test harness (section 16)
# enforces that this file, the embedded digests, and the actual script
# contents stay in lockstep.
#
# Established from: main @ 711c23b (post Session 3 integration), computed
# with `sha256sum pixel-dev-setup.sh pixel-apps-setup.sh` on 2026-07-11.
#
# Update procedure (any change to either script):
#   1. sha256sum pixel-dev-setup.sh pixel-apps-setup.sh
#   2. update this file AND the embedded digests in pixel-bootstrap.sh
#      in the SAME commit as the script change (the harness goes red otherwise)
#   3. note the commit the digests were established from, above
#
# Format: <sha256>  <script name>   (sha256sum format)
bc9b49fa953ae5511fd576180f83e00f350788b6f5d3cab295a649ede14a292f  pixel-dev-setup.sh
844d81db780f9ffec8f7d49388f9261429d34d826737ea2abbe533b7892eb619  pixel-apps-setup.sh
