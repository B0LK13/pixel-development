# Bootstrap integrity pins — SHA-256 digests (schema v1).
#
# Scope: the three root-level shell entry points. pixel-bootstrap.sh verifies
# network-fetched setup scripts against the digests embedded in ITSELF (kept
# in lockstep with this file — harness section 16); this manifest additionally
# pins pixel-bootstrap.sh itself, putting the anchor's digest on record for
# the verified install flow (README.md §1) and for release verification.
#
# Established from: main @ 711c23b (dev/apps digests, 2026-07-11); the anchor
# pin tracks the working tree and is re-pinned by any bootstrap change.
#
# Update procedure (any change to a pinned script — one lockstep workflow):
#   1. update the embedded digests in pixel-bootstrap.sh (dev/apps only)
#   2. refresh this manifest in the SAME commit
#   3. all three must agree — harness §16 and CI go red otherwise
# (Session 5: `bash scripts/update-bootstrap-checksums.sh --write` performs
#  steps 1+2 deterministically; `--check` is the gate.)
#
# Format: <sha256>  <script name>  (sha256sum format, sorted by name)
844d81db780f9ffec8f7d49388f9261429d34d826737ea2abbe533b7892eb619  pixel-apps-setup.sh
3542e200217f3ec57d0b5e83df5e413d24929d45048f0f89f1022c71882d9aa5  pixel-bootstrap.sh
bc9b49fa953ae5511fd576180f83e00f350788b6f5d3cab295a649ede14a292f  pixel-dev-setup.sh
