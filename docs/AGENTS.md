# Scoped agent rules for `docs/`

- Keep command references executable and aligned with scripts/workflows.
- Keep evidence path references valid.
- Avoid stale numeric claims unless they are updated from current gate outputs.
- Update related contract docs when behavior changes.
- Prefer concise normative language for policy/contract docs.
- Cross-reference authoritative files instead of duplicating conflicting rules.
